void lisp_print(node n,tree &T) {
  if (/* `n' es `Lambda' ... */) return;

  if (/* `n' es hoja ...*/) /* imprime `n' ... */;
  else /* imprime `(' n lisp_print(s1) 
	  lisp_print(s2) ... lisp_print(sn) `)' */
}
